# llmemo

## APM

```bash
# Skill
apm install owner/repo/path/to/skill --target agent-skills

# MCP / Serena
apm install --mcp serena --target codex -- \
  serena start-mcp-server --project-from-cwd --context=codex

# dry run + `--dry-run`
```